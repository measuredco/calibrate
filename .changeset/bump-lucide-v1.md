---
"@measured/calibrate-core": patch
---

Bump `lucide` from `0.577.0` to `1.14.0`. Major version of upstream — usage in `Icon` is limited to the `icons` registry export and is unaffected. Pinned exact (no caret) so future minors land via Dependabot review rather than implicit `pnpm install` upgrades.
