---
"@measured/calibrate-core": patch
---

Text: `data-measured` paragraphs at `size="lg"` now use the tight measure (`--clbr-typography-measure-tight`) instead of the default measure. Large prose was reading long; tighter measure keeps line lengths balanced at that size.
