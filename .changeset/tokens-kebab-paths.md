---
"@measured/calibrate-tokens": minor
---

Normalise dotted token paths in `clbr.*.tokens.json` to kebab-case (matching the CSS custom property form) and emit contexts with the default context first, preserving source order instead of sorting alphabetically. Token values are unchanged; the dotted key shape and key order are.
