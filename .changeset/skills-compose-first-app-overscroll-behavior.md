---
"@measured/calibrate-skills": patch
---

Compose-first full-page example now passes `appOverscrollBehavior="none"` on the Root, matching app-shell layout best practice (prevents iOS/macOS rubber-band overscroll at the document root). Both flavors updated together.
