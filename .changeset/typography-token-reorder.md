---
"@measured/calibrate-tokens": patch
---

Reorder typography token groups in the semantic source (and matching dist JSON output) to a more readable order — `paragraph` moves ahead of `leading`. Token names and values are unchanged.
