---
"@measured/calibrate-tokens": patch
---

Clarify the `layout.spacing.vertical.*` and `container.gutter.narrow` token descriptions to state explicitly that these are the responsive layout variants. Token names and values are unchanged.
