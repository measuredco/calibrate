---
"@measured/calibrate-tokens": patch
---

Order the semantic `effect.*` keys (`opacity`, `shadow`, `stroke`, `filter`) so the dist JSON reads in a more natural order. Token names, values, and descriptions are unchanged.
