---
---

Internal: Storybook a11y addon now runs the cumulative WCAG 2.2 AA ruleset plus axe's `best-practice` recommendations (with `region` disabled — page-level rule that doesn't fit isolated component stories). No version bump — story-file change only, no shipped surface.
