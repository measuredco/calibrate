---
"@measured/calibrate-core": minor
"@measured/calibrate-react": minor
---

Surface: add `contentTheme` prop. Mirrors Poster — accepts `"light" | "dark"` and emits `data-clbr-content-theme` on the host when provided. Use to lock content rendered inside a Surface to an absolute theme regardless of the inherited `prefers-color-scheme`.
