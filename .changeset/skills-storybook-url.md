---
"@measured/calibrate-skills": patch
---

Update the live component catalog URL in `compose-first` and `custom-with-tokens` skills from `calibrate-storybook.msrd.dev` (retired) to `calibrate.msrd.dev/storybook` (the bundled docs-site deploy).
