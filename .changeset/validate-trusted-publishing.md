---
"@measured/calibrate-core": patch
---

Smoke-test Trusted Publishing — no code changes. Incrementing alpha to confirm the OIDC publish path works end-to-end after the bootstrap NPM_TOKEN was removed.
