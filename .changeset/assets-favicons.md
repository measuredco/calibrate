---
"@measured/calibrate-assets": minor
---

Add favicon assets — `favicon.ico`, `favicon.svg` (light/dark adaptive via `prefers-color-scheme`), `apple-touch-icon.png`, and maskable PWA icons at 192px and 512px. Each is exposed via its own `./favicons/<name>` export path. See package README for `<head>` markup and PWA manifest usage.
