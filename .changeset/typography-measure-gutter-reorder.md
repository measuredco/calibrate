---
"@measured/calibrate-tokens": patch
---

Reorder a few semantic token keys so the dist JSON reads in a more natural order: `typography.measure` now lists `default` before `tight`, and `layout.container.gutter` lists `default` before `narrow`. Token names, values, and descriptions are unchanged.
