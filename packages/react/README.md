# @measured/calibrate-react

React adapter for Calibrate. Typed React wrappers over the core custom-element library.

Wrappers are generated from the structured `CLBR_*_SPEC` contract in `@measured/calibrate-core` via the `@measured/calibrate-adapter` walker, so the React surface always tracks the underlying component IR.
