# @measured/calibrate-react

React adapter for the Calibrate design system.

Thin typed React surface over the `clbr-*` custom elements. Wrappers are
generated from the structured `CLBR_*_SPEC` contract in `@measured/calibrate-core`.

Status: scaffold only. Generator and wrappers are not yet wired.
