# @measured/calibrate-adapter

Codegen for Calibrate framework adapters. Walks the structured `CLBR_*_SPEC` contracts in `@measured/calibrate-core` and emits framework-specific wrappers (currently React; Vue planned).

Internal/private — `@measured/calibrate-react` is the public consumption contract for React; consumers don't depend on this package directly.
